// screens/ai_coaching.dart
import 'package:flutter/material.dart';

class AICoachingScreen extends StatelessWidget {
  const AICoachingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AI Career Coaching')),
      body: const Center(
        child: Text('AI Career Coaching Screen - Coming Soon!'),
      ),
    );
  }
}
