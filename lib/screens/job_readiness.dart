// screens/job_readiness.dart
import 'package:flutter/material.dart';

class JobReadinessScreen extends StatelessWidget {
  const JobReadinessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Job Readiness')),
      body: const Center(
        child: Text('Job Readiness Screen - Coming Soon!'),
      ),
    );
  }
}
